<h1>
  <%= current_user.name %>
</h1>
      <%#= image_tag current_user.image, :class => "profile-img" %>
      <%= link_to gravatar_for(current_user), current_user, class: "profile-img" %>

<div>
  <%= link_to "view my profile", current_user %>
</div>
<span>
  <%= pluralize(current_user.posts.count, "post") %>
</span>
