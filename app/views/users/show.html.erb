<% if current_user %>
  <div class="container text-center help">
    <h1>My Posts</h1>
    <% @posts.each do |post| %>
      <div class="container">
        <%= post.title %>
        <%= post.description %>
        <%= post.keyword %>
        <%= link_to 'Edit post', '/posts/' + post.id.to_s + '/edit' if current_user.id == @user.id %>
      </div>
      <% end %>


  </div>
<% else %>

<div class="container text-center help">
  <h1>My Posts</h1>
  <% @posts.each do |post| %>
    <div class="container">
      <%= post.title %>
      <%= post.description %>
      <%= post.keyword %>
      <%= link_to 'Edit post', '/posts/' + post.id.to_s + '/edit' if current_user.id == @user.id %>
    </div>
    <% end %>


</div>

<% end %>
<%#= link_to "Edit", edit_job_path(@job), class: "btn btn-sm btn-default" if @current_user.id == @job.owner_id %>
